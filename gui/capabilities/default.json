{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows IPC (invoke/event) and window dragging on the main window.\nAdd a label here whenever you add a window \u2014 without it no invoke gets through at all, so the window appears but nothing works",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主画面と付属ウィンドウ (設定) に IPC (invoke/event) と窓の移動を許可する。\n札を足し忘れた窓は invoke が一切通らず、画面は出るのに何も動かない",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}

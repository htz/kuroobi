{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主画面に IPC (invoke/event) と窓の移動を許可する。\n窓を増やすときは札を足すこと — 足さないと invoke が一切通らず、画面は出るのに何も動かない",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}

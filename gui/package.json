{
  "name": "kuroobi-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "test -z \"$(ls src-ui/js/*.js 2>/dev/null)\" && tsc --noEmit || (echo 'src-ui/js に .js が残っています (TypeScript 化前の残骸)。Vite はそちらを読むので消してください。' && exit 1)"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
